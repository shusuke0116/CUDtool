<!Doctype html>
<html>
<head>
  <meta charset="utf-8">
  <title>CUD学習ツール</title>
</head>
<script>
function choiceColor(code) {
  const phrase = document.querySelector("#phrase");
  phrase.style.color = "#" + code;
}
</script>
<style type="text/css">
p.ex{
  font-size: 1.5rem;
}
p#phrase{
  color:<%= "#" + col[0] %>;
  font-weight: bold;
}
.a0,.a1,.a2,.a3{
  font-weight: bold;
  text-align:center;
}
.a0{
  color:<%= "#" + col[0] %>;
  background-color:<%= "#" + bcol[0] %>;
}
.a1 {
  color:<%= "#" + col[1] %>;
  background-color:<%= "#" + bcol[1] %>;
}
.a2 {
  color:<%= "#" + col[2] %>;
  background-color:<%= "#" + bcol[2] %>;
}
.a3 {
  color:<%= "#" + col[3] %>;
  background-color:<%= "#" + bcol[3] %>;
}
#que,#eva,#acho{
  font-size: 1.5rem;
}
#cho{
  font-size: 1.75rem;
}
#sen,#ano{
  border: 2px solid;
  border-color:#c8c8c8;
  font-size: 1.75rem;
  width: <%= 1.75*16 %>rem;
}
#sen{
  color:<%= "#" + col[0] %>;
  background-color:<%= "#" + bcol[0] %>;
  text-align:center;
}
#ano,#sim{
  text-align:left;
}
.button{
  font-size: 1.5rem;
}

</style>
<body>
  <p class="ex">2部：色の組み合わせ[ 1.色相 → 2.明度 → 3.彩度 ]</p>
  <hr>
  <h1>1. <%= textdata[0].item %></h1>
  <hr>

  <form action="/question/4/answer" method="post">
  <div id="que">
    <p><%= textdata[0].que %></p>
    <img src="images/sikisou.png" alt="色相">
    <details>
      <summary>色相とは</summary>
      <p>図のように赤、緑、青などの色味の種類のことです。色覚異常では、色相で別となっている２つの色の区別がつかない場合があります。</p>
    </details>
  </div>
  <div id="cho">
    <% let c; 
      for (let row of choices){ 
        if(row.ccode == col[0]) c = "checked";
        else c = ""; %>
    <font color="<%= '#' + row.ccode %>">
      <input type="radio" name="choice" id="<%= row.ccode %>" value="<%= row.id %>" onclick="choiceColor(this.id)" <%= c %>><%= row.name %>
    </font>
    <input type="hidden" name="gb" value="<%= row.gb %>">
    <% } %>
  </div>
  <div id="sen">
    <p id="phrase">カラーユニバーサルデザイン</p>
  </div>    
  
  <div id="button">
    <input type="hidden" name="id" value="<%= textdata[0].id %>">
    <input type="hidden" name="bcol" value="<%= bcol[0] %>">
    <p><input type="submit" value="評価" class="button"></p>
  </div>
  </form>

  <% if(e == 1){ %>
    <div id="acho">
      選択した色：<%= col[4] %>
    </div>
    <div id="ano">
      ＜別色覚視点＞<br>
      <div id="sim">
        通常：
        <div class="a0">
          カラーユニバーサルデザイン<br>
        </div>
        P型：
        <div class="a1">
          カラーユニバーサルデザイン<br>
        </div>
        D型：
        <div class="a2">
          カラーユニバーサルデザイン<br>
        </div>
        S型：
        <div class="a3">
          カラーユニバーサルデザイン<br>
        </div>
      </div>
    </div>
    <div id="eva">
      <% if(gb == 0) { %> 
        <p><%= textdata[0].bad %></p>
        <p>ポイント：<%= textdata[0].adv %></p>
        <p>別の選択肢を選んでください</p>
      <% }else{ %> 
        <p><%= textdata[0].good %></p>  
        <p>ポイント：<%= textdata[0].adv %></p>
        <hr>
        <p><a href="<%= '/question/5/' + col[0] %>">次の項目へ</a></p>
      <% } %>
    </div>
  <% } %>

</body>
</html>